ON YOUR EC2 - RUN THIS NOW
===========================

nano .env

PASTE THESE 3 LINES (with YOUR actual AWS credentials):
--------------------------------------------------------
AWS_REGION=us-east-2
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

(Replace the XXX with your actual AWS IAM access key and secret)

SAVE: Ctrl+X, then Y, then Enter


THEN RUN:
---------
docker-compose restart routing-service

sleep 10

curl -X POST "http://localhost:8002/api/v1/optimizer/optimize?hub_id=1&use_ai_explanation=true"


THAT'S IT. The Kimi K2.5 model is already configured in the code as:
BEDROCK_MODEL_ID = "moonshotai.kimi-k2.5"

The API format is already correct (OpenAI-compatible).

You just need your AWS credentials in the .env file.
